{{!-- using templating library to create the divs with the articles themselves
this is what has been declared in the schema
use handlebars with; 
article.id
article.title
the json object that gets served with the server will be templetated on the front end --}}